version: '3.8'

services:
  app:
    build: .
    image: molecule-pipeline
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      PYTHONUNBUFFERED: "1"
